apiVersion: v1
kind: Service
metadata:
  name: {{ app.name }}
  labels:
    service: {{ app.name }}
spec:
  type: NodePort
  selector:
    app: {{ app.name }}-app
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: {{ app.name }}
